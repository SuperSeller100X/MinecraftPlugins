name: EasyMending CI

on:
  push:
    paths:
      - 'EasyMending/**'
      - '.github/workflows/easymending.yml'
  pull_request:
    paths:
      - 'EasyMending/**'
      - '.github/workflows/easymending.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: EasyMending
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Set up Java 25
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'
          cache: maven
      - name: Test and package
        run: mvn -B -ntp clean verify
      - name: Upload plugin jar
        uses: actions/upload-artifact@v4
        with:
          name: EasyMending
          path: EasyMending/target/EasyMending-*.jar
          if-no-files-found: error
